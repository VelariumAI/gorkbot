{
  "name": "bash_command_analyzer",
  "description": "Analyzes bash command patterns, calculates success rates, and provides tool analytics summary based on command history data.",
  "author": "ToolForge",
  "version": "1.0.0",
  "entry_point": "main.py",
  "parameters": {
    "type": "object",
    "properties": {
       "args": { "type": "string", "description": "Arguments for the tool" }
    }
  }
}