{
  "name": "safe_cleanup",
  "description": "Safely scans and optionally cleans old logs/project artifacts with age filtering, dry-run, and HITL-safe JSON output",
  "author": "ToolForge",
  "version": "1.0.0",
  "entry_point": "main.py",
  "parameters": {
    "type": "object",
    "properties": {
       "args": { "type": "string", "description": "Arguments for the tool" }
    }
  }
}