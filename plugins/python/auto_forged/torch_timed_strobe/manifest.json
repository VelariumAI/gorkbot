{
  "name": "torch_timed_strobe",
  "description": "Controls the device flashlight (torch) with a timed sequence: turns off for a specified duration, then strobes (rapid on/off) for another duration. Uses termux-torch from Termux:API.",
  "author": "ToolForge",
  "version": "1.0.0",
  "entry_point": "main.py",
  "parameters": {
    "type": "object",
    "properties": {
       "args": { "type": "string", "description": "Arguments for the tool" }
    }
  }
}