{
  "name": "shell_system_utils",
  "description": "Executes bash commands and reads system files to gather system information. Supports running shell commands, reading system files like /proc/version, and retrieving environment variables.",
  "author": "ToolForge",
  "version": "1.0.0",
  "entry_point": "main.py",
  "parameters": {
    "type": "object",
    "properties": {
       "args": { "type": "string", "description": "Arguments for the tool" }
    }
  }
}