{
  "name": "bash_sanitizer",
  "description": "Validates and sanitizes bash commands to prevent path escapes, injections, and /tmp issues in Termux/Gorkbot environments. Rejects dangerous patterns while fixing common /tmp escapes.",
  "author": "ToolForge",
  "version": "1.0.0",
  "entry_point": "main.py",
  "parameters": {
    "type": "object",
    "properties": {
       "args": { "type": "string", "description": "Arguments for the tool" }
    }
  }
}