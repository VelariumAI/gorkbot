{
  "name": "system_info",
  "description": "Retrieves system information including date/time, disk usage, active processes, and environment context. Consolidates shell, system monitoring, and env var queries into a single plugin.",
  "author": "ToolForge",
  "version": "1.0.0",
  "entry_point": "main.py",
  "parameters": {
    "type": "object",
    "properties": {
       "args": { "type": "string", "description": "Arguments for the tool" }
    }
  }
}