{
  "name": "bash_pattern_analyzer",
  "description": "Analyzes bash command execution patterns to identify success rates, common commands, and potential issues based on Gorkbot audit data",
  "author": "ToolForge",
  "version": "1.0.0",
  "entry_point": "main.py",
  "parameters": {
    "type": "object",
    "properties": {
       "args": { "type": "string", "description": "Arguments for the tool" }
    }
  }
}