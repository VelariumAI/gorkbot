{
  "name": "bash_curl_fetch",
  "description": "Fallback web fetcher using bash/curl to bypass dependency issues. Use when web_fetch or similar tools fail due to missing packages (lynx, scrapling) or OOM on installations.",
  "author": "ToolForge",
  "version": "1.0.0",
  "entry_point": "main.py",
  "parameters": {
    "type": "object",
    "properties": {
       "args": { "type": "string", "description": "Arguments for the tool" }
    }
  }
}