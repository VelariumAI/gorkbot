{
  "name": "system_optimize",
  "description": "Performs system optimization tasks: memory reclaim via sync, disk usage analysis, ollama AI commands, and cache cleanup. Generalizes the recurring Termux optimization pattern.",
  "author": "ToolForge",
  "version": "1.0.0",
  "entry_point": "main.py",
  "parameters": {
    "type": "object",
    "properties": {
       "args": { "type": "string", "description": "Arguments for the tool" }
    }
  }
}