{
  "name": "check_ram_usage",
  "description": "Checks system RAM and swap usage via /proc/meminfo, reports statistics in GiB and percentages, and issues notifications when available RAM drops below 50% or swap usage exceeds 95% following the observed monitoring pattern.",
  "author": "ToolForge",
  "version": "1.0.0",
  "entry_point": "main.py",
  "parameters": {
    "type": "object",
    "properties": {
       "args": { "type": "string", "description": "Arguments for the tool" }
    }
  }
}