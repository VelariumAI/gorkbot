{
  "name": "termux_screenshot",
  "description": "Takes a screenshot in Termux Android environment and saves it to visible storage (~/storage/shared/Download/). Handles one-time storage permission setup automatically.",
  "author": "ToolForge",
  "version": "1.0.0",
  "entry_point": "main.py",
  "parameters": {
    "type": "object",
    "properties": {
       "args": { "type": "string", "description": "Arguments for the tool" }
    }
  }
}