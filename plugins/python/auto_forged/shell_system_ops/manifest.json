{
  "name": "shell_system_ops",
  "description": "Executes safe shell commands, reads system files, and manages common system operations with error handling for Termux/permission issues",
  "author": "ToolForge",
  "version": "1.0.0",
  "entry_point": "main.py",
  "parameters": {
    "type": "object",
    "properties": {
       "args": { "type": "string", "description": "Arguments for the tool" }
    }
  }
}