{
  "name": "system_diagnostics",
  "description": "Generalized tool to run bash monitoring commands, list processes, check disk/CPU/memory usage, execute demo tasks (ls, date, system stats), and simulate Android sensor/screenshot steps. Reads JSON params from stdin and returns consolidated output.",
  "author": "ToolForge",
  "version": "1.0.0",
  "entry_point": "main.py",
  "parameters": {
    "type": "object",
    "properties": {
       "args": { "type": "string", "description": "Arguments for the tool" }
    }
  }
}