{
  "name": "system_info_gatherer",
  "description": "A Gorkbot plugin that safely runs bash commands and reads system files, handling common patterns like environment variables and /proc filesystem access",
  "author": "ToolForge",
  "version": "1.0.0",
  "entry_point": "main.py",
  "parameters": {
    "type": "object",
    "properties": {
       "args": { "type": "string", "description": "Arguments for the tool" }
    }
  }
}