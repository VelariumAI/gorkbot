{
  "name": "disk_cleanup",
  "description": "Generalized safe disk cleanup: removes *.tmp/*.old files from Download dirs, cleans Gorkbot logs and caches, runs package autoclean, then reports df -h usage. Accepts configurable paths and patterns via JSON input.",
  "author": "ToolForge",
  "version": "1.0.0",
  "entry_point": "main.py",
  "parameters": {
    "type": "object",
    "properties": {
       "args": { "type": "string", "description": "Arguments for the tool" }
    }
  }
}