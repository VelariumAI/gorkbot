{
  "name": "system_status_probe",
  "description": "Generates structured system status reports for Gorkbot including runtime, tool inventory, clock verification, and operational gaps using the observed reporting pattern.",
  "author": "ToolForge",
  "version": "1.0.0",
  "entry_point": "main.py",
  "parameters": {
    "type": "object",
    "properties": {
       "args": { "type": "string", "description": "Arguments for the tool" }
    }
  }
}