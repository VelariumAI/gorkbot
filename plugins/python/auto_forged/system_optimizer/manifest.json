{
  "name": "system_optimizer",
  "description": "Performs system optimization tasks including memory reclamation, cache cleaning, disk usage analysis, and resource monitoring. Supports drop_caches, sync, disk usage checks, and returns system stats.",
  "author": "ToolForge",
  "version": "1.0.0",
  "entry_point": "main.py",
  "parameters": {
    "type": "object",
    "properties": {
       "args": { "type": "string", "description": "Arguments for the tool" }
    }
  }
}