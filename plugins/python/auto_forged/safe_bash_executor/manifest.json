{
  "name": "safe_bash_executor",
  "description": "Generalized, reliable bash command executor for Gorkbot diagnostics, tool testing, and shell operations. Improves on native bash tool by adding timeout, safe execution, output capture, and optional logging to /tmp/gorkbot_test/. Handles high-usage patterns like echo tests, mkdir for test dirs, resource checks (top, free). Input JSON: {"command": "bash command string", "log": true/false (default false), "timeout": seconds (default 30)}",
  "author": "ToolForge",
  "version": "1.0.0",
  "entry_point": "main.py",
  "parameters": {
    "type": "object",
    "properties": {
       "args": { "type": "string", "description": "Arguments for the tool" }
    }
  }
}