{
  "name": "termux_storage_get",
  "description": "Generalized reusable tool that correctly invokes termux-storage-get with a directory argument (e.g. pictures, downloads) to open the Android storage picker, imports the selected file into the sandbox, and returns the local path. Fixes the previous -t flag bug.",
  "author": "ToolForge",
  "version": "1.0.0",
  "entry_point": "main.py",
  "parameters": {
    "type": "object",
    "properties": {
       "args": { "type": "string", "description": "Arguments for the tool" }
    }
  }
}