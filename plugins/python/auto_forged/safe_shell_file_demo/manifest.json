{
  "name": "safe_shell_file_demo",
  "description": "Executes safe shell commands and reads system files for demo purposes. Supports running bash commands, reading system files like /proc/version, and retrieving system information.",
  "author": "ToolForge",
  "version": "1.0.0",
  "entry_point": "main.py",
  "parameters": {
    "type": "object",
    "properties": {
       "args": { "type": "string", "description": "Arguments for the tool" }
    }
  }
}