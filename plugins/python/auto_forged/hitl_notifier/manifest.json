{
  "name": "hitl_notifier",
  "description": "Starts a background monitor that watches Gorkbot logs for [HITL] events and sends notifications (generalized Python version of the bash tailing pattern)",
  "author": "ToolForge",
  "version": "1.0.0",
  "entry_point": "main.py",
  "parameters": {
    "type": "object",
    "properties": {
       "args": { "type": "string", "description": "Arguments for the tool" }
    }
  }
}