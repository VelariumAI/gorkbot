{
  "name": "bash_executor",
  "description": "Executes bash commands and returns output as JSON. Generalizes shell command execution pattern.",
  "author": "ToolForge",
  "version": "1.0.0",
  "entry_point": "main.py",
  "parameters": {
    "type": "object",
    "properties": {
       "args": { "type": "string", "description": "Arguments for the tool" }
    }
  }
}