{
  "name": "system_memory_reclaim",
  "description": "Reclaims system memory by flushing disk buffers and dropping page cache. Supports sync-only (safer) or full cache drop. Useful for freeing RAM under pressure.",
  "author": "ToolForge",
  "version": "1.0.0",
  "entry_point": "main.py",
  "parameters": {
    "type": "object",
    "properties": {
       "args": { "type": "string", "description": "Arguments for the tool" }
    }
  }
}