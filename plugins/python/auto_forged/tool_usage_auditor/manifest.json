{
  "name": "tool_usage_auditor",
  "description": "Analyzes tool usage statistics (calls, success rates, categories) to detect duplicates vs complementary tools, assess risk, and generate registry recommendations like merge/prune/no-action",
  "author": "ToolForge",
  "version": "1.0.0",
  "entry_point": "main.py",
  "parameters": {
    "type": "object",
    "properties": {
       "args": { "type": "string", "description": "Arguments for the tool" }
    }
  }
}