{
  "name": "send_sms",
  "description": "Sends an SMS message on Android/Termux. Tries direct send with termux-sms-send, falls back to am intent (opens composer). Input JSON: {"number": "+1234567890", "message": "Hello"}",
  "author": "ToolForge",
  "version": "1.0.0",
  "entry_point": "main.py",
  "parameters": {
    "type": "object",
    "properties": {
       "args": { "type": "string", "description": "Arguments for the tool" }
    }
  }
}