{
  "name": "ensure_home_dir",
  "description": "Resolves 'chdir $HOME: no such file or directory' errors by validating and setting HOME environment variable with fallback paths",
  "author": "ToolForge",
  "version": "1.0.0",
  "entry_point": "main.py",
  "parameters": {
    "type": "object",
    "properties": {
       "args": { "type": "string", "description": "Arguments for the tool" }
    }
  }
}