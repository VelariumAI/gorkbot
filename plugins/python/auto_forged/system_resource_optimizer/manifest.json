{
  "name": "system_resource_optimizer",
  "description": "Generalized tool that monitors system resources (RAM/storage), clears caches, identifies large directories, and returns structured summaries based on observed patterns from system_monitor and structured_bash usage.",
  "author": "ToolForge",
  "version": "1.0.0",
  "entry_point": "main.py",
  "parameters": {
    "type": "object",
    "properties": {
       "args": { "type": "string", "description": "Arguments for the tool" }
    }
  }
}