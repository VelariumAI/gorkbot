{
  "name": "apply_terminal_theme",
  "description": "Generalized reusable tool that applies a temporary terminal prompt theme by constructing and simulating a colored PS1 export (non-destructive), accepting color_code and theme_name parameters from input JSON.",
  "author": "ToolForge",
  "version": "1.0.0",
  "entry_point": "main.py",
  "parameters": {
    "type": "object",
    "properties": {
       "args": { "type": "string", "description": "Arguments for the tool" }
    }
  }
}