{
  "name": "system_health_checker",
  "description": "Analyzes system state, tool usage metrics, detects failures (especially SanitizerReject), calculates health scores, and provides recovery recommendations",
  "author": "ToolForge",
  "version": "1.0.0",
  "entry_point": "main.py",
  "parameters": {
    "type": "object",
    "properties": {
       "args": { "type": "string", "description": "Arguments for the tool" }
    }
  }
}