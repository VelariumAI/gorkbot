{
  "name": "gorkbot_skill_generator",
  "description": "Analyzes tool usage patterns and generates SKILL.md files to fix recurring failures based on SENSE trace data",
  "author": "ToolForge",
  "version": "1.0.0",
  "entry_point": "main.py",
  "parameters": {
    "type": "object",
    "properties": {
       "args": { "type": "string", "description": "Arguments for the tool" }
    }
  }
}