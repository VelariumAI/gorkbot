{
  "name": "ping_test",
  "description": "Tests network connectivity by pinging a target host using the ping command. Useful for verifying internet access and reachability.",
  "author": "ToolForge",
  "version": "1.0.0",
  "entry_point": "main.py",
  "parameters": {
    "type": "object",
    "properties": {
       "args": { "type": "string", "description": "Arguments for the tool" }
    }
  }
}