{
  "name": "sense_skill_generator",
  "description": "Generates proactive SKILL.md files from failure trace patterns to prevent recurring errors like sanitizer rejects and MCP timeouts",
  "author": "ToolForge",
  "version": "1.0.0",
  "entry_point": "main.py",
  "parameters": {
    "type": "object",
    "properties": {
       "args": { "type": "string", "description": "Arguments for the tool" }
    }
  }
}