{
  "name": "file_manager",
  "description": "Manages files, environment variables, and basic system operations (delete files, update bashrc, clear caches, restart services)",
  "author": "ToolForge",
  "version": "1.0.0",
  "entry_point": "main.py",
  "parameters": {
    "type": "object",
    "properties": {
       "args": { "type": "string", "description": "Arguments for the tool" }
    }
  }
}