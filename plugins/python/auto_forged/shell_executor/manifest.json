{
  "name": "shell_executor",
  "description": "Executes shell commands and reads system files, providing a unified interface for shell and system management operations. Supports bash commands, file reads, and process listing.",
  "author": "ToolForge",
  "version": "1.0.0",
  "entry_point": "main.py",
  "parameters": {
    "type": "object",
    "properties": {
       "args": { "type": "string", "description": "Arguments for the tool" }
    }
  }
}