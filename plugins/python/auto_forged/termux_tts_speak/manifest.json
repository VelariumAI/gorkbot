{
  "name": "termux_tts_speak",
  "description": "Speaks the provided text using Termux TTS engine after setting music stream volume to maximum. Accepts 'message' parameter in input JSON.",
  "author": "ToolForge",
  "version": "1.0.0",
  "entry_point": "main.py",
  "parameters": {
    "type": "object",
    "properties": {
       "args": { "type": "string", "description": "Arguments for the tool" }
    }
  }
}