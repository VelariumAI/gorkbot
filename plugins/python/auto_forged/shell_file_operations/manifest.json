{
  "name": "shell_file_operations",
  "description": "Executes shell commands, performs file operations, and monitors system resources with structured output parsing",
  "author": "ToolForge",
  "version": "1.0.0",
  "entry_point": "main.py",
  "parameters": {
    "type": "object",
    "properties": {
       "args": { "type": "string", "description": "Arguments for the tool" }
    }
  }
}